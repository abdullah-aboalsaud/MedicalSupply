<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/product_nav"
    app:startDestination="@id/productsFragment">

    <fragment
        android:id="@+id/detailsProductFragment"
        android:name="com.example.medicalsupply.product.fragments.DetailsProductFragment"
        android:label="DetailsProductFragment"
        tools:layout="@layout/fragment_details_product"
        >
        <argument
            android:name="productId"
            app:argType="string" />
    </fragment>
    <fragment
        android:id="@+id/productsFragment"
        android:name="com.example.medicalsupply.product.fragments.ProductsFragment"
        android:label="ProductsFragment"
        tools:layout="@layout/fragment_products"
        >
        <action
            android:id="@+id/action_productsFragment_to_detailsProductFragment"
            app:destination="@id/detailsProductFragment"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@android:anim/fade_out"
            app:popEnterAnim="@android:anim/slide_in_left"
            app:popExitAnim="@android:anim/slide_out_right" />
    </fragment>
</navigation>